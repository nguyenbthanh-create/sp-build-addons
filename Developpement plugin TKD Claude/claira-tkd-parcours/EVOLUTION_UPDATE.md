### 2026-07-28 — Clickable grade interface

- Removed SVG hotspot interaction from the plugin.
- Each grade is now a direct clickable button rendered in `includes/shortcodes.php`.
- Modal behavior is managed by `assets/js/script.js` only.
- This update simplifies the interface and removes obsolete hotspot files.
